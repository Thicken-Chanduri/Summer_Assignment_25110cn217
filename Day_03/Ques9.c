#include<stdio.h>
int main()
{
    int a,b;
    printff("Enter the number");    
    scanf("%d",&a);
    for(b=2;b,a/2;b++)
    {
        if(a%b==0)
        {
            printf("%d is not a prime number",a);
            break;
        }
        else{
            pritnf("%d is a prime number",a);
            break;
        }
        return 0;
    }

}
